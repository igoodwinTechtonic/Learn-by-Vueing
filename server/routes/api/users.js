const router = require('express').Router();

// router.route('/:id')
//   .get(async (req, res) => {
//     const response = await collection('users').findOne({ "_id": req.params.id })
//     if (response) res.send(response)
//     else res.status(202).send(null)
//   })

module.exports = router;